/* Local copy of EmailJS SDK (minimal placeholder) - CDN fetch blocked in this environment.
   You should download the real file from https://cdn.emailjs.com/sdk/3.2.0/email.min.js and replace this file.
*/
console.warn(
  "Local email.min.js placeholder loaded — replace with real EmailJS SDK from CDN if needed.",
);
window.emailjs = window.emailjs || {};
window.emailjs.send =
  window.emailjs.send ||
  function () {
    return Promise.reject(
      new Error("EmailJS placeholder: real SDK not installed"),
    );
  };
window.emailjs.init =
  window.emailjs.init ||
  function () {
    console.warn("EmailJS placeholder init called");
  };
